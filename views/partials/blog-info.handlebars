<article class="blog">
  <div class="title">
    <a href="{{blog_url}}" target="_blank">{{title}}</a>
    <span>({{blog_url}})</span>
  </div>
  <div class="meta">

    <a href="/blog/{{id}}">{{comments.length}}
      {{format_plural "comment" comments.length}}</a>
  </div>
</article>